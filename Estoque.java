public class Estoque {
    public boolean verificarDisponibilidade(String produto, int quantidade) {
        // Lógica complexa de verificação de estoque
        System.out.println("Verificando disponibilidade de " + quantidade + " unidades de " + produto);
        return true;  // Assume que o estoque é sempre suficiente para este exemplo
    }
}
